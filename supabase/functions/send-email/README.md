Deploy this function as `send-email` and add these Supabase Edge Function secrets:

- GMAIL_CLIENT_ID
- GMAIL_CLIENT_SECRET
- GMAIL_REFRESH_TOKEN
- GMAIL_FROM_EMAIL

The caller must be authenticated. Gmail credentials never appear in the browser.
