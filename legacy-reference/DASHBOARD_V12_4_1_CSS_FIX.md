# Dashboard V12.4.1 CSS Fix

Fixes the V12.4 dashboard stylesheet placement. The dashboard CSS is now loaded in the main application stylesheet rather than inside the report print template.

No database migration is required.
